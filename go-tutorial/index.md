# Go 教程 · 面向 C/C++ 程序员

> 本教程假设你已经熟悉 C/C++(指针、栈/堆、值传递与引用、编译链接、手动内存管理)。
> Go 对 C/C++ 程序员最"亲切"——它有真正的指针、值类型、结构体、编译成原生机器码;
> 但也有很多需要看清底层的地方:**每个操作到底发生了什么**——
> 例如 `a = b` 对值类型是逐字节拷贝、对 slice/map 却是拷贝"表头"?
> slice 的 `len/cap/底层数组指针` 三元组如何工作?goroutine 为什么便宜?逃逸分析如何决定栈还是堆?

## 阅读顺序(建议)

| # | 章节 | 一句话说明 |
|---|------|-----------|
| 01 | [运行模型:编译、链接、运行时](01-运行模型.md) | 编译成静态原生二进制,自带运行时(调度器 + GC) |
| 02 | [基础语法(与 C/C++ 对照)](02-基础语法.md) | 包、`:=`、多返回值、无隐式转换、类型在后 |
| 03 | [⭐ 变量与内存模型:`a = b` 到底发生了什么](03-变量与内存模型.md) | 一切皆值拷贝、指针是显式的、逃逸分析决定栈/堆 |
| 04 | [数据类型与内存布局](04-数据类型.md) | 定长数值、`struct` 内存对齐、`string` 只读头、`interface` 双字 |
| 05 | [控制流](05-控制流.md) | 只有 `for`、`switch` 无 fallthrough、`defer` 的机制 |
| 06 | [函数、方法与调用栈:参数传递的真相](06-函数与调用栈.md) | 全是值传递、值接收者 vs 指针接收者、可增长的 goroutine 栈 |
| 07 | [复合数据结构的内部实现](07-复合数据结构.md) | ⭐ slice 三元组与扩容、map 的哈希桶、channel 的环形队列 |
| 08 | [类型系统:结构体、接口、方法集](08-面向对象与类型系统.md) | 组合优于继承、接口的 `(type, value)` 双字、泛型 |
| 09 | [错误处理与垃圾回收](09-错误处理与资源管理.md) | `error` 是值、`panic/recover`、并发三色标记 GC |
| 10 | [并发模型:goroutine 与 channel](10-并发模型.md) | GMP 调度器、CSP、`select`、内存模型与 `sync` |
| 11 | [标准库中的算法与数据结构](11-标准库算法.md) | `sort`、`slices`/`maps`(泛型)、`container/heap`、`container/list` |
| 12 | [常用第三方库速览](12-常用第三方库.md) | 生态定位:web 框架、序列化、日志、测试等 |

## 全教程贯穿的几个"底层视角"

1. **Go 里赋值和传参永远是"值拷贝"**——理解这一点后,slice/map/channel 的"看起来像引用"的行为都能解释:它们拷贝的是一个很小的"表头结构",表头里含有指向底层数据的指针。
2. **指针是显式且安全的**:有 `&`、`*`,但没有指针算术;编译器通过**逃逸分析**决定变量放栈还是堆,GC 负责回收。
3. **并发是语言内建的**:`go f()` 启动一个由运行时调度到 OS 线程上的 goroutine,`channel` 是带锁的类型安全队列——不是库,是语言原语。

> 每章末尾都有「上一章 / 下一章」导航,正文中相关概念处会互相跳转。
