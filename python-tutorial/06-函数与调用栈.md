# 函数与调用栈

这是全教程最需要"换脑子"的一章。C/C++ 里参数传递只有两种物理机制:按值(拷贝一份到被调栈帧)和按指针(拷贝一个地址)。C++ 又加了引用(`&`,给同一块内存起别名)。你脑子里那套"值 vs 指针 vs 引用"的模型非常清晰——但它**不能直接套到 Python**。Python 只有一种传参方式,我们必须精确地说清它是什么。

## def 创建的是一个对象

先纠正一个直觉:C 里函数是一段代码的地址(函数指针指向 text 段),编译期就定死。Python 里 `def` 是一条**运行时执行的语句**,它在执行时创建一个 `function` 对象放到堆上,再把这个对象绑定到名字:

```python
def add(a, b):
    return a + b

print(type(add))          # <class 'function'>
print(add.__code__)       # 编译好的 code 对象(字节码在这)
print(add.__name__)       # 'add'
add_alias = add           # 只是多一个名字指向同一个函数对象
```

一个 `function` 对象里装着:`__code__`(不可变的字节码及元数据)、`__defaults__`(默认参数值)、`__globals__`(它能看到的全局命名空间)、`__closure__`(闭包捕获的变量)。函数是"一等公民"——能赋值、能当参数传、能当返回值、能塞进容器,因为它就是个普通对象。

## 参数传递的真相:按对象引用(按共享传递)

Python 的传参机制叫 **call by object reference**,也叫 **call by sharing**(按共享传递)。一句话:**实参和形参指向堆上的同一个对象;传递过程拷贝的是引用(指针),不是对象本身。**

这听起来像 C 的"传指针",但有个关键差异:在 C 里传指针,你在函数内对形参 `p = ...` 重新赋值不影响调用方,但你能 `*p = ...` 改内容;而 Python 里你手上根本没有"指针的指针",你无法让调用方的名字改指向别处。用两个维度看最清楚:

| 维度 | Python 的行为 |
|------|--------------|
| **能否改变调用方变量的指向?** | 不能。形参重新赋值只改本地名字绑定 |
| **能否改变对象的内容?** | 能,但前提是对象**可变**(list/dict/set/自定义对象) |

### 维度一:重新赋值形参 —— 永远不影响调用方

```python
def rebind(x):
    # x 原本和调用方的实参指向同一对象;
    # 下面这行让本地名字 x 改指向一个新对象,调用方毫不知情
    x = 999
    print("函数内:", x)

n = 1
rebind(n)
print("函数外:", n)     # 1 —— 没变
```

内存图(调用瞬间,`n` 和 `x` 指向同一个 int 对象;赋值后 `x` 改指向新对象):

```
调用时:                        x = 999 之后:
  n ---> [int 1]                 n ---> [int 1]
  x ---> [int 1]                 x ---> [int 999]  (新对象)
```

这正是为什么 Python **无法**写出 C 的 `void swap(int *a, int *b)` 那种"通过参数改调用方变量"的函数。想交换,只能返回新值:`a, b = b, a` 再由调用方接收。

### 维度二:原地修改可变对象 —— 调用方看得见

```python
def mutate(lst):
    # 没有重新绑定 lst,而是调用它的方法修改它指向的那个对象
    lst.append(4)      # 原地修改,不产生新 list
    print("函数内:", lst)

data = [1, 2, 3]
mutate(data)
print("函数外:", data)   # [1, 2, 3, 4] —— 变了!
```

内存图(只有一个 list 对象,两个名字都指向它):

```
  data --\
          >---> [list: 1,2,3] --append--> [list: 1,2,3,4]
  lst  --/
```

这和 C 传指针后 `*p` 改内容效果一致。区别只在于:Python 里"能不能改"取决于**对象本身可不可变**,而不是取决于你怎么声明参数。

### 可变 vs 不可变实参对照

同一段"改一下参数"的代码,对可变和不可变实参表现相反:

```python
def try_change(obj):
    if isinstance(obj, list):
        obj += [99]        # list 的 += 是原地 extend(改内容)
    else:
        obj += 99          # int 的 += 生成新对象再绑定给本地 obj

a = [1, 2]
try_change(a)
print(a)          # [1, 2, 99] —— 可变对象被改了

b = 10
try_change(b)
print(b)          # 10 —— 不可变对象,调用方不受影响
```

陷阱就在这里:`obj += [99]` 和 `obj += 99` 看着都是 `+=`,但对 list 是原地修改(等价 `obj.extend`,`__iadd__` 返回自身),对 int 是"新建 + 重绑定"(int 没有原地加法,`__iadd__` 缺失时退化为 `obj = obj + 99`)。可变性决定一切。

### 再来几个例子,彻底看透

**例 1:一次调用里同时演示两个维度**——同一个函数,对 `a` 重新赋值(看不见),对 `b` 原地改(看得见):

```python
def f(a, b):
    a = a + [100]     # 对 a:新建一个 list 再绑定给本地名 a → 调用方看不到
    b.append(100)     # 对 b:调方法原地改它指向的对象 → 调用方看得到
    print("函数内 a =", a, " b =", b)   # [1, 100]  [1, 100]

x = [1]
y = [1]
f(x, y)
print("函数外 x =", x)   # [1]        —— 没变(只改了本地绑定)
print("函数外 y =", y)   # [1, 100]   —— 变了(改了对象内容)
```

**例 2:自定义对象也一样**——改属性 = 改内容(可见);重新绑定形参 = 只改本地名(不可见):

```python
class Account:
    def __init__(self, bal):
        self.balance = bal

def touch(acc):
    acc.balance += 50     # 改对象的属性,就是改内容 → 可见
    acc = Account(0)      # 让本地名 acc 改指向一个新对象 → 调用方无感
    print("函数内 acc.balance =", acc.balance)   # 0

a = Account(100)
touch(a)
print("函数外 a.balance =", a.balance)   # 150 —— 属性被改了;a 仍指向原来那个对象
```

**例 3:那我怎样才能真的改调用方变量的指向?** 两条路:

```python
# 路子一:把新值 return 回去,由调用方自己重新绑定(最 Pythonic)
def older(age):
    return age + 1

age = 30
age = older(age)     # 调用方接收返回值,自己改指向
print(age)           # 31

# 路子二:包一层可变容器,改容器里的元素(相当于 C 的 int**:传"指针的指针")
def set_value(box, val):
    box[0] = val     # 改的是容器内容,可见
holder = [None]
set_value(holder, 42)
print(holder[0])     # 42
```

**例 4:最经典的一处易混**——`lst = lst + [x]` 与 `lst += [x]` 只差一点,结果相反:

```python
def rebind(lst):
    lst = lst + [9]   # 先算出一个【新 list】,再绑定给本地名 lst → 调用方看不到
def inplace(lst):
    lst += [9]        # 等价 lst.extend([9]),原地改同一个对象 → 调用方看得到

m = [1]
rebind(m)
print(m)              # [1]      —— 没变
n = [1]
inplace(n)
print(n)              # [1, 9]   —— 变了
```

`lst + [9]` 是"造新对象",`lst += [9]` 是"原地改"——一字之差,天壤之别。这正是前面 `+=` 陷阱的另一面。

> 记忆口诀:**传的永远是引用,拷贝的永远是引用本身(一个指针),从不拷贝对象。改指向不外传,改内容看可变。**

## 参数种类:位置、关键字、默认、*args、**kwargs

Python 的参数系统比 C 灵活得多。函数签名里各种符号的完整形态:

```python
def f(pos1, pos2, /, both, *args, kw_only, **kwargs):
    #   ^^^^^^^^^^   /   ^^^^  ^^^^^  ^^^^^^^   ^^^^^^^^
    ...
```

- `/` 之前:**仅限位置**参数(caller 不能用 `pos1=...` 传),3.8+;
- `/` 与 `*` 之间:位置或关键字都行(最常见);
- `*args`:把多余的位置实参收集成一个**元组**;
- `*` 之后(或 `*args` 之后):**仅限关键字**参数,必须写 `kw_only=...`;
- `**kwargs`:把多余的关键字实参收集成一个**字典**。

```python
def demo(a, b, *args, sep="-", **kwargs):
    print(f"a={a} b={b}")
    print("args =", args)        # 元组
    print("kwargs =", kwargs)    # 字典
    print("sep =", sep)

demo(1, 2, 3, 4, sep="|", x=10, y=20)
# a=1 b=2
# args = (3, 4)
# kwargs = {'x': 10, 'y': 20}
# sep = |
```

底层上,`*args` 会**分配一个新元组**装那些额外实参,`**kwargs` 会**分配一个新字典**。调用点的 `*` / `**` 是反向操作(解包):

```python
nums = [1, 2, 3]
print(*nums)             # 等价 print(1, 2, 3),把列表解包成位置实参
opts = {"sep": "/"}
print("a", "b", **opts)  # 把字典解包成关键字实参 -> sep="/"
```

## 默认参数:在 def 执行时求值一次(经典陷阱)

C++ 的默认实参在**每次调用**时求值。Python 相反:默认值在 **`def` 语句执行时求值一次**,结果存进函数对象的 `__defaults__`,以后每次调用共享这同一个对象。对不可变默认值没问题,对可变默认值就是著名的坑:

```python
def bad(item, bucket=[]):     # 这个 [] 只在定义时创建一次!
    bucket.append(item)
    return bucket

print(bad(1))    # [1]
print(bad(2))    # [1, 2] —— 惊了!同一个 list 被反复复用
print(bad(3))    # [1, 2, 3]

print(bad.__defaults__)   # ([1, 2, 3],) —— 默认值就存在这里,被改花了
```

原因:`bucket` 的默认值是定义时创建的**那一个** list 对象,存在 `__defaults__` 里。每次不传 `bucket`,形参就绑定到这同一个对象,`append` 一直往它身上加。

正确写法是用 `None` 哨兵,在函数体内新建:

```python
def good(item, bucket=None):
    if bucket is None:
        bucket = []           # 每次调用都新建,互不干扰
    bucket.append(item)
    return bucket

print(good(1))   # [1]
print(good(2))   # [2]
```

同理,别把 `time.time()`、`{}`、`[]` 之类当默认值期望"每次调用重新算"——它只算一次。

## 返回多值:其实是一个元组

C 要返回多个值得靠输出参数(指针)或包一个 struct。Python 看着能"返回多个值",底层其实是**打包成一个元组再解包**:

```python
def divmod2(a, b):
    return a // b, a % b      # 逗号建元组,等价 return (a//b, a%b)

q, r = divmod2(17, 5)         # 右边是元组,左边序列解包
print(q, r)                   # 3 2

t = divmod2(17, 5)
print(type(t), t)             # <class 'tuple'> (3, 2)
```

没有真正的"多返回值",只有"返回一个元组 + 解包赋值"。所以它的成本是分配一个小元组(CPython 对小元组有缓存优化)。

## 闭包与 cell 变量

当内层函数引用了外层函数的局部变量,就形成**闭包**。这里 Python 又和 C 分道扬镳:C 的局部变量在栈帧上,函数返回即失效,你没法安全返回指向它的指针。Python 里被闭包捕获的变量会被放进堆上的 **cell 对象**,即使外层函数已返回,变量依然存活:

```python
def make_counter():
    count = 0                 # 被内层引用,提升为 cell 变量
    def inc():
        nonlocal count        # 声明:要修改的是外层的 count,不是新建本地
        count += 1
        return count
    return inc                # 外层返回后,count 依然活着

c = make_counter()
print(c(), c(), c())          # 1 2 3

print(c.__closure__)          # (<cell at ...: int object at ...>,)
print(c.__closure__[0].cell_contents)   # 3 —— cell 里存着当前值
```

内存图:`count` 不在栈上,而在一个 cell 里,闭包 `inc` 通过 `__closure__` 持有对该 cell 的引用:

```
   make_counter 返回后,栈帧销毁,但:

   c (function inc) --__closure__--> ( cell )
                                       |
                                       v
                                    [int 3]   <- 在堆上,存活
```

`nonlocal` 声明"我要写外层作用域的变量";`global` 声明"我要写模块级全局变量"。不写这两个而直接赋值,Python 会把它当成**新建本地变量**(因为赋值默认创建本地绑定),从而报 `UnboundLocalError`——这是新手常见错误:

```python
x = 0
def wrong():
    x += 1          # UnboundLocalError:x 被当成本地变量,但读时还没绑定
def right():
    global x
    x += 1          # OK
```

## 调用栈:frame 对象

C 的栈帧是编译期固定布局的一块连续内存,函数返回即弹出。CPython 的调用栈由 **frame 对象**组成,每次调用创建一个 frame,里面有局部变量表、指向上一帧的链接等——它是**堆上的对象**(CPython 3.11 起对帧做了内联优化,但语义上仍可当对象访问),甚至能在运行时反射:

```python
import sys

def inner():
    frame = sys._getframe()           # 当前帧
    print("当前函数:", frame.f_code.co_name)      # 'inner'
    print("本地变量:", frame.f_locals)             # {'frame': ...}
    print("调用者:", frame.f_back.f_code.co_name)  # 'outer' —— 上一帧

def outer():
    inner()

outer()
```

| frame 属性 | 含义(对照 C) |
|-----------|--------------|
| `f_locals` | 本地变量字典(C 里是栈上一块无名内存) |
| `f_globals` | 所在模块的全局命名空间 |
| `f_back` | 指向调用者的帧(即调用栈的上一层) |
| `f_code` | 正在执行的 code 对象 |
| `f_lineno` | 当前行号 |

这套可反射的帧结构正是 traceback、调试器、`logging` 能打印出完整调用链的基础。代价是比 C 栈帧重。

### 递归深度限制

C 递归深到爆是栈溢出(段错误)。CPython 为防止 C 层栈真的溢出,设了一个**软限制**,超过就抛 `RecursionError` 而不是崩溃:

```python
import sys
print(sys.getrecursionlimit())    # 通常 1000

def rec(n):
    return 1 if n == 0 else rec(n - 1) + 1

# rec(100000) 会抛 RecursionError,而不是让进程段错误
sys.setrecursionlimit(3000)       # 可调大,但调太大仍可能真的爆 C 栈
```

Python **没有尾调用优化**,深递归请改写成迭代或显式用栈。

## 一等函数与装饰器原理

因为函数是对象,它能被别的函数接收、包装、返回。**装饰器**就是这套能力的语法糖:`@deco` 加在 `def foo` 上,等价于 `foo = deco(foo)`——用 `deco` 的返回值(通常是个包装函数)替换原名字。

```python
import functools, time

def timer(func):
    @functools.wraps(func)        # 把 func 的 __name__/__doc__ 拷到 wrapper
    def wrapper(*args, **kwargs):  # 用 *args/**kwargs 透传任意参数
        t0 = time.perf_counter()
        result = func(*args, **kwargs)   # 调用被包装的原函数
        print(f"{func.__name__} 耗时 {time.perf_counter()-t0:.6f}s")
        return result
    return wrapper                 # 返回包装函数,替换掉原来的名字

@timer
def work(n):
    return sum(range(n))

# 上面完全等价于:work = timer(work)
work(1_000_000)
print(work.__name__)   # 'work' —— 多亏 functools.wraps,否则会是 'wrapper'
```

底层看清楚:`@timer` 执行时立刻调用 `timer(work)`,得到 `wrapper` 这个新函数对象,再把它绑定到名字 `work`。此后调用 `work(...)` 实际调用的是 `wrapper`,它内部再转调原函数(通过闭包 `__closure__` 抓住的 `func`)。这就是"包装"——没有魔法,全是一等函数 + 闭包。

`functools.wraps` 很重要:不加它,`work.__name__` 会变成 `'wrapper'`,traceback 和文档都会指向包装函数,给调试带来困惑。

## 小结

- `def` 在运行时创建 `function` 对象;函数是一等公民。
- 传参是**按对象引用/按共享**:拷贝引用(指针),不拷贝对象。改形参指向不外传;原地改对象内容,可变对象才外传。这套两维度模型是理解 Python 传参的核心。
- 默认参数在 `def` 执行时求值**一次**,可变默认值会被跨调用复用——用 `None` 哨兵规避。
- 返回"多值"实为返回元组再解包;闭包变量存在堆上的 cell 里,靠 `__closure__` 持有,`nonlocal`/`global` 控制写外层/全局。
- 调用栈由可反射的 frame 对象组成(`f_locals`/`f_back`),有递归软限制,无尾调用优化;装饰器 = 一等函数 + 闭包的语法糖。

[← 上一章:控制流](05-控制流.md) · [返回目录](index.md) · [下一章:复合数据结构](07-复合数据结构.md) →
