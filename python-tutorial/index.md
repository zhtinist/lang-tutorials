# Python 教程 · 面向 C/C++ 程序员

> 本教程假设你已经熟悉 C/C++(指针、栈/堆、值传递与引用、编译链接、手动内存管理)。
> 因此我们不重复"什么是变量、什么是循环",而是把重点放在:**每一个操作在底层到底发生了什么**——
> 例如 `a = b` 时,`a` 拿到的是对象的引用(指针)还是新分配了一块内存?可变对象与不可变对象在内存里有何区别?
> `list`、`dict` 内部是怎么实现的?GIL 为什么存在?

## 阅读顺序(建议)

| # | 章节 | 一句话说明 |
|---|------|-----------|
| 01 | [运行模型:CPython 如何执行你的代码](01-运行模型.md) | 源码 → 字节码 → 解释器循环,对象都在堆上 |
| 02 | [基础语法(与 C/C++ 对照)](02-基础语法.md) | 缩进即作用域、动态类型、一切皆表达式 |
| 03 | [⭐ 变量与内存模型:`a = b` 到底发生了什么](03-变量与内存模型.md) | 名字→对象的引用绑定,`id()`、引用计数、小整数缓存 |
| 04 | [数据类型与底层布局](04-数据类型.md) | `int` 是大对象、`float`、`str`、`bytes` 的内存结构 |
| 05 | [控制流](05-控制流.md) | 真值测试、`for` 的迭代器协议、`match` |
| 06 | [函数与调用栈:参数传递的真相](06-函数与调用栈.md) | "传对象引用"、默认参数陷阱、闭包与 cell |
| 07 | [复合数据结构的内部实现](07-复合数据结构.md) | `list` 扩容、`dict` 开放寻址、`set`、`tuple` |
| 08 | [面向对象与类型系统](08-面向对象与类型系统.md) | 一切皆对象、`__dict__`、MRO、鸭子类型、`__slots__` |
| 09 | [异常处理与垃圾回收](09-错误处理与资源管理.md) | 引用计数 + 分代 GC、循环引用、`with` 与 RAII 对照 |
| 10 | [并发模型:GIL、线程、协程](10-并发模型.md) | 为什么多线程算不快、`asyncio` 事件循环、多进程 |
| 11 | [标准库中的算法与数据结构](11-标准库算法.md) | `collections`、`heapq`、`bisect`、`itertools`、`sort` 的 Timsort |
| 12 | [常用第三方库速览](12-常用第三方库.md) | numpy、requests、pandas、pydantic 等的定位 |

## 全教程贯穿的几个"底层视角"

1. **CPython 里没有"栈上的对象"**:除了少数被解释器内部优化的情况,所有 Python 值(哪怕一个整数 `3`)都是堆上分配的 `PyObject`,变量只是指向它的名字。
2. **变量 = 名字,不是内存槽**:C 的 `int a` 是一块 4 字节内存;Python 的 `a` 是命名空间字典里的一个键,值是指向对象的指针。
3. **"可变 vs 不可变"决定一切别名行为**:理解这一点就能预测所有 `=`、函数传参、拷贝的结果。

> 每章末尾都有「上一章 / 下一章」导航,正文中相关概念处会互相跳转。
